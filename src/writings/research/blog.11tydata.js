export default {
	eleventyComputed: {
	/*	eleventyNavigation: (data) => ({
			isDynamic: true,
			key: data.pagination.pageNumber === 0 ? "Blog" : "Page " + data.pagination.pageNumber,
			parent: data.pagination.pageNumber === 0 ? "Writings" : "Blog",
		}),*/
	},
};
