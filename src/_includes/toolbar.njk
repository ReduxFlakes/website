<header class="navigator">
    <a href="/" class="brand">reduxflakes</a>
    <nav class="navigator-nav">
        {% for item in navigator %}
            <a href="{{item.url}}">
                {{item.title}}
            </a>
        {% endfor %}
    </nav>
</header>