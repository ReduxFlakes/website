<header class="main-header">
    <h1>
        {% if header_title %}
            {{header_title}}
        {% else %}
            {{title}}
        {% endif %}
    </h1>
    <h1>{{meta.title}}</h1>
    <p>{{meta.description}}</p>
</header>