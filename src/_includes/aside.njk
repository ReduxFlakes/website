<aside class="aside stack">
    <section class="stack experience-section">
        <h2>Experiences</h2>
        {% for item in experiences %}
            <a href="{{item.url}}" style="background-image:url(/public/img/expo/{{item.img}})">{{item.title}}</a>
        {% endfor %}
    </section>
    {% include "components/button-list.njk" %}

</aside>